import { makeStyles } from "@material-ui/core/styles";

export const useStyles = makeStyles({
  AppButton: {
    width: "150px",
    height: "40px",
  },
  AppSwitches: {
    display: "flex",
    margin: "10px",
    alignItems: "center",
  },
  AppCategoryChanger: {
    display: "flex",
    flexDirection: "column",
  },
});
